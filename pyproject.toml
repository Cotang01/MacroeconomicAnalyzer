[project]
name = "MacroeconomicAnalyzer"
version = "0.1.0"
description = "Test task for StaffIT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "tabulate>=0.9.0",
    "types-tabulate>=0.9.0.20241207",
]
