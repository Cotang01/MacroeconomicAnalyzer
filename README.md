### Test task for StaffIT

#### Packet manager used: uv

#### Script usage example:

`python main.py --files economic1.csv economic2.csv --report average-gdp`

#### To run tests and see code coverage (included in pytest.ini):

`pytest`